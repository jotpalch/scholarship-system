## Source: user-supplied snapshot of `root@jotp:~/naass/docker-compose.prod-db-monitoring.yml` on prod DB-VM
## Captured: 2026-05-06 during Phase 1 audit Stage 0.4
## Purpose: prod-side ground truth for Branch C (Alloy + Cross-VM) and Branch E (Deploy Pipeline) audit
##
version: '3.8'

# Monitoring Services for Production DB-VM
# Separated from main DB services for modular deployment
# Exposes ports for Prometheus to scrape metrics

services:
  # =========================================================================
  # GRAFANA ALLOY - Unified Telemetry Collector
  # =========================================================================
  alloy:
    image: grafana/alloy:latest
    container_name: scholarship_alloy_prod_db
    volumes:
      - ./monitoring/config/alloy/prod-db-vm.alloy:/etc/alloy/config.alloy:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - scholarship_prod_db_network
    environment:
      - MONITORING_SERVER_URL=${MONITORING_SERVER_URL}
    command: run --server.http.listen-addr=0.0.0.0:12345 /etc/alloy/config.alloy
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "5m"
        max-file: "3"

  # =========================================================================
  # NODE EXPORTER - System Metrics
  # =========================================================================
  node-exporter:
    image: prom/node-exporter:latest
    container_name: node_exporter_prod_db
    command:
      - '--path.rootfs=/host'
      - '--path.procfs=/host/proc'
      - '--path.sysfs=/host/sys'
      - '--collector.filesystem.mount-points-exclude=^/(sys|proc|dev|host|etc)($$|/)'
    volumes:
      - /:/host:ro,rslave
    ports:
      - "9100:9100"
    networks:
      - scholarship_prod_db_network
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "5m"
        max-file: "3"

  # =========================================================================
  # POSTGRESQL EXPORTER - Database Metrics
  # =========================================================================
  postgres-exporter:
    image: prometheuscommunity/postgres-exporter:latest
    container_name: postgres_exporter_prod
    environment:
      DATA_SOURCE_NAME: "postgresql://${POSTGRES_USER:-scholarship_user}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB:-scholarship_db}?sslmode=require"
    ports:
      - "9187:9187"
    networks:
      - scholarship_prod_db_network
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "5m"
        max-file: "3"

  # Note: Redis Exporter not needed - Redis is on AP-VM

networks:
  scholarship_prod_db_network:
    external: true
    name: scholarship_prod_db_network
