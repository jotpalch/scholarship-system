# Loki Configuration for Scholarship System Monitoring
# Multi-tenant setup: staging and production

# Enable multi-tenancy
auth_enabled: true

server:
  http_listen_port: 3100
  grpc_listen_port: 9096
  log_level: info

# Distributor configuration
distributor:
  ring:
    kvstore:
      store: inmemory

# Ingester configuration
ingester:
  lifecycler:
    address: 127.0.0.1
    ring:
      kvstore:
        store: inmemory
      replication_factor: 1
    final_sleep: 0s
  chunk_idle_period: 1h
  max_chunk_age: 1h
  chunk_target_size: 1048576
  chunk_retain_period: 30s
  wal:
    enabled: true
    dir: /loki/wal

# Query configuration
querier:
  max_concurrent: 10

# Query frontend configuration
query_range:
  results_cache:
    cache:
      embedded_cache:
        enabled: true
        max_size_mb: 100

# Schema configuration
schema_config:
  configs:
    - from: 2024-01-01
      store: tsdb
      object_store: filesystem
      schema: v13
      index:
        prefix: index_
        period: 24h

# Storage configuration
storage_config:
  tsdb_shipper:
    active_index_directory: /loki/tsdb-index
    cache_location: /loki/tsdb-cache
    cache_ttl: 24h
  filesystem:
    directory: /loki/chunks

# Compactor configuration
compactor:
  working_directory: /loki/compactor
  compaction_interval: 10m
  retention_enabled: false

# Limits configuration
limits_config:
  # Loki used to log a flood of "entry has timestamp too old" errors on
  # first deploy because Alloy on DB-VM replays whatever Docker has
  # cached for each container — easily weeks old after a long-running
  # staging stack. Bumping this to 720h (30d) suppresses that noise
  # without weakening retention (separate setting in retention_period).
  reject_old_samples: true
  reject_old_samples_max_age: 720h
  ingestion_rate_mb: 4
  ingestion_burst_size_mb: 6
  max_label_name_length: 1024
  max_label_value_length: 2048
  max_label_names_per_series: 30
  max_cache_freshness_per_query: 10m
  split_queries_by_interval: 15m

# Ruler configuration (disabled - no AlertManager)
# ruler:
#   storage:
#     type: local
#     local:
#       directory: /loki/rules
#   rule_path: /loki/rules-temp
#   ring:
#     kvstore:
#       store: inmemory
#   enable_api: false
