[project]
name = "webhook-bridge"
version = "1.0.0"
description = "Grafana → GitHub repository_dispatch webhook bridge"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.32",
    "httpx>=0.27",
]
